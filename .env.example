# ============================================================
# CodeRewind Environment Configuration
# ============================================================
# Copy this file to .env and fill in your values
# Required variables are marked with [REQUIRED]

# ============================================================
# Application URL [REQUIRED]
# ============================================================
# This is the public URL where your app is accessible
# Used for OAuth callback URLs
#
# Development: http://localhost:5173
# Production: https://your-domain.com
PUBLIC_APP_URL=http://localhost:5173

# ============================================================
# GitHub OAuth Configuration [REQUIRED for GitHub integration]
# ============================================================
# Create an OAuth App at: https://github.com/settings/developers
#
# Settings for your OAuth App:
# - Homepage URL: {PUBLIC_APP_URL}
# - Authorization callback URL: {PUBLIC_APP_URL}/auth/github/callback
# - Required scopes: read:user, repo
#
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# ============================================================
# GitLab OAuth Configuration [OPTIONAL - for GitLab integration]
# ============================================================
# Create an OAuth App at: https://gitlab.com/-/user_settings/applications
#
# Settings for your OAuth App:
# - Redirect URI: {PUBLIC_APP_URL}/auth/gitlab/callback
# - Required scopes: read_user, read_api, read_repository
# - Confidential: Yes (required for server-side auth)
#
# Leave empty or remove these variables if you don't want GitLab integration
GITLAB_CLIENT_ID=
GITLAB_CLIENT_SECRET=

# ============================================================
# Optional: Self-hosted GitLab Instance
# ============================================================
# If using a self-hosted GitLab instance, you can override the default URLs
# by modifying the OAuth endpoints in the code
#
# GITLAB_URL=https://gitlab.yourcompany.com

# ============================================================
# Development Notes
# ============================================================
# 1. Never commit your .env file to version control
# 2. For production deployment, set these as environment variables
# 3. GitHub tokens don't expire by default
# 4. GitLab tokens expire in 2 hours but refresh tokens are supported
# 5. Make sure callback URLs in OAuth apps EXACTLY match PUBLIC_APP_URL
